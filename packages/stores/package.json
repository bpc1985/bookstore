{
  "name": "@bookstore/stores",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "tsc --noEmit",
    "clean": "rm -rf node_modules dist"
  },
  "dependencies": {
    "@bookstore/types": "workspace:*",
    "@bookstore/api": "workspace:*",
    "zustand": "^5.0.9"
  },
  "peerDependencies": {
    "react": ">=18.0.0"
  }
}
